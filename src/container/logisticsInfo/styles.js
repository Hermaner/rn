import { StyleSheet } from 'react-native';
import { st, deviceH } from '../../utils';

const styles = StyleSheet.create({
  ...st,
  box: {
    flex: 1,
    height: deviceH,
  },
});
export default styles;
